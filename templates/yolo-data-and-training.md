# YOLO Data and Training Plan

仅当类别语义或复杂外观不能被稳定颜色/几何规则解决时使用。本模板不虚构数据集，不自动下载未知来源图片。

## 是否真的需要模型

- 传统视觉失败的证据：
- YOLO 需要识别的类别和输出：
- 是否可用颜色、ROI、尺寸或状态机减少模型负担：

## 数据采集

- 类别表与“非目标/空场景”定义：
- 赛场距离、视角、尺度、遮挡、方向和光照覆盖：
- 每类采集来源、授权状态和保存位置：
- 难例：反光、相似背景、部分遮挡、快速运动、空格网：

## 标注与划分

- 标注格式与工具：
- 边界框/类别命名规则：
- 训练、验证、测试集划分及避免同一连续视频泄漏：
- 标注抽查、漏标和错标处理：

## 训练、部署与验收

- 训练输入尺寸、类别顺序、评价指标：
- 误检/漏检代价与置信度、IOU、ROI、时序过滤策略：
- 转换后的 `.mud` 模型路径、标签一致性和 MaixCAM 加载测试：
- `dual_buff` 选择：实时显示可启用；严格同步闭环须关闭或补偿一帧延迟。
- 无模型、加载失败或低置信度时的降级显示与安全动作：
